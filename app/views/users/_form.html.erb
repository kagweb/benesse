<%= simple_form_for(@user) do |f| %>

  <%= f.error_notification %>
  <%= f.input :username %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.input :password_confirmation, required: true%>
  <%= f.association :department, collection: Department.all %>
  <%= f.submit %>

<% end %>
